To start backend:
 - use pip to install necessary packages (detailed in 'backend > requirements.txt')
 - run server.py

To start frontend:
 - move frontend files to a new react app
 - npm install to download node_modules
 - npm start in that react app

If necessary:
 - goto 'backend > config.ini' to change server port